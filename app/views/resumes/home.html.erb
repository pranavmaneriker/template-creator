<% provide(:title, "Resume Home") %>

<%= button_to 'Create Resume', resumes_createpage_path , :method => "get", class:"btn btn-primary" %> 

<%= button_to 'Existing Resume', resumes_viewlist_path , :method => "get", class:"btn btn-primary" %> 